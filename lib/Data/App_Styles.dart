
import 'package:flutter/material.dart';

class AppStyles {
  static TextStyle one(
    BuildContext context,{
      double ? fontsize,
      color? color,
      FontWeight?
      FontWeight,
    }
  )
}